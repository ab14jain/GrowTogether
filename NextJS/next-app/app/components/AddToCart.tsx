'use client'
import React from 'react'

const AddToCart = () => {
  return (
    <div>
        <button
            onClick={() => alert('Button clicked!')}>
            Click Me
        </button>
    </div>
  )
}

export default AddToCart